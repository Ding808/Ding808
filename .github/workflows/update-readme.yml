name: Update GitHub Stats

on:
  schedule:
    - cron: "0 * * * *"  
  workflow_dispatch: 

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v2
       - uses: Ding808/github-activity-readme@main
         env:
           GITHUB_TOKEN: ${{secrets.GH_STATS_TOKEN}}
         
